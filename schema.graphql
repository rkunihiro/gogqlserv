# GraphQL schema
#

type User {
    id: Int!
    name: String!
}

type Query {
    users: [User!]!
    findUser(
        id: Int!
    ): User
}

type Mutation {
    addUser(
        name: String!
    ): User!
}
